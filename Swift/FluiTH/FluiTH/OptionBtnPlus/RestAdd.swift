import Foundation
import SwiftUI
struct RestAdd: View {
    var body: some View {
        Text("vista generar reposo")
            .font(.caption)
            .foregroundColor(.gray)
    }
}
struct RestAdd_Previews: PreviewProvider {
    static var previews: some View {
        RestAdd()
    }
}
