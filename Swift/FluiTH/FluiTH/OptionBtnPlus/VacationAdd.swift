import Foundation
import SwiftUI
struct VacationAdd: View {
    var body: some View {
        Text("vista generar vacaciones")
            .font(.caption)
            .foregroundColor(.gray)
    }
}
struct VacationAdd_Previews: PreviewProvider {
    static var previews: some View {
        VacationAdd()
    }
}
