import Foundation
import SwiftUI
struct LicencesAdd: View {
    var body: some View {
        Text("vista nueva licencia")
            .font(.caption)
            .foregroundColor(.gray)
    }
}
struct LicencesAdd_Previews: PreviewProvider {
    static var previews: some View {
        LicencesAdd()
    }
}
