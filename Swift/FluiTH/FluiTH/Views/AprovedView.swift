import Foundation
import SwiftUI
struct AprovedView: View {
    var body: some View {
        Text("Vista de Aprobados")
            .font(.largeTitle)
            .navigationTitle("Aprobados") // Si usas NavigationStack
    }
}
