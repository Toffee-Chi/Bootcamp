import Foundation
import SwiftUI
struct BirthDay: View {
    var body: some View {
        Text("vista correo cumple jaja")
            .font(.caption)
            .foregroundColor(.gray)
    }
}
struct BirthDay_Previews: PreviewProvider {
    static var previews: some View {
        BirthDay()
    }
}
