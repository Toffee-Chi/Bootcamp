import Foundation
import SwiftUI
struct EmployeesAdd: View {
    var body: some View {
        Text("vista nuevos funcionarios")
            .font(.caption)
            .foregroundColor(.gray)
    }
}
struct EmployeesAdd_Previews: PreviewProvider {
    static var previews: some View {
        EmployeesAdd()
    }
}
